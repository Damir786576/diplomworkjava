spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ads_db
    username: postgres
    password: damir_999
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true